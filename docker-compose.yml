version: "3.8"

services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NODE_ENV: production
      PORT: "5000"
      SESSION_SECRET: ${SESSION_SECRET:-change-me-to-a-secure-random-string}
